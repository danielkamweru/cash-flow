{
  "name": "wealth-loop-backend",
  "private": true,
  "scripts": {
    "start": ".venv/bin/python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000",
    "start:prod": ".venv/bin/python -m uvicorn app.main:app --host 0.0.0.0 --port 8000",
    "seed": ".venv/bin/python -m app.seed"
  }
}
